#include <stdio.h>

int main()
{
    int n;
    printf("Enter static boundary:");
    scanf("%d", &n);
    int a[n], i;
    printf("Enter %d array elements:", n) for (i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
}